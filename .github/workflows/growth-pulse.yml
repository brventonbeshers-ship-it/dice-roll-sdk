name: Scheduled SDK Health Check

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

concurrency:
  group: scheduled-dice-roll-sdk-health-check
  cancel-in-progress: true

jobs:
  scheduled-verify:
    name: Verify dice-roll-sdk from npm
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Prepare clean environment
        run: mkdir -p /tmp/dice-roll-sdk-check

      - name: Initialize project
        working-directory: /tmp/dice-roll-sdk-check
        run: npm init -y

      - name: Fetch dice-roll-sdk from registry
        working-directory: /tmp/dice-roll-sdk-check
        run: npm install dice-roll-sdk

      - name: Confirm SDK require succeeds
        working-directory: /tmp/dice-roll-sdk-check
        run: node -e "const sdk = require('dice-roll-sdk'); console.log('Scheduled health check: dice-roll-sdk OK');"

      - name: Show dependency tree
        working-directory: /tmp/dice-roll-sdk-check
        run: npm ls --all
